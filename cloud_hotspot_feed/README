This is an OpenWrt package feed containing cloud-hotspot packages.

To use these packages, add the following line to the feeds.conf
in the OpenWrt buildroot:

  src-link cloud_hotspot ${OPENWRT_DIR}/../cloud_hotspot_feed
  
Update the feed:

  ./scripts/feeds update cloud_hotspot
  
Activate the package:

  ./scripts/feeds install -a -p cloud_hotspot
  
The cloud_hotspot packages should now appear in menuconfig.
